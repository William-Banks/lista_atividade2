# Declaração da lista de números inicial
lista = [8, 10, 64, 5, 97, 26, 18, 5, 9, 7]
# Exibição da lista inicial na tela
print(lista)

# Para exibir o dobro de todos os números da lista, multiplica-se cada um deles por 2
lista_dobro = [8 * 2, 10 * 2, 64 * 2, 5 * 2, 97 * 2, 26 * 2, 18 * 2, 5 * 2, 9 * 2, 7 * 2]
# Exibição da nova lista na tela
print(lista_dobro)